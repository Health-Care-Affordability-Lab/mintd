# {{ full_project_name }}

Research project: {{ project_name }}.

## Overview

This repository contains the analysis code and outputs for {{ project_name }}.

## Project Structure

- `data/`: Project-specific data files (raw, intermediate, final)
- `code/`: Analysis scripts ({{ language | default('Python') }})
- `results/figures/`: Generated figures
- `results/tables/`: Generated tables
- `notebooks/`: Jupyter/R notebooks for exploration
- `docs/`: Documentation
- `references/`: Reference materials and literature
- `tests/`: Test files
- `logs/`: Execution logs

## Requirements

{% if language == 'r' %}
Install R dependencies with renv:
```r
install.packages("renv")
renv::restore()
```
{% elif language == 'stata' %}
No additional package manager required. Ensure Stata is installed and configured.
{% else %}
Install Python dependencies:
```bash
pip install -r requirements.txt
```
{% endif %}

## Analysis

Run analysis scripts from the `code/` directory:

{% if language == 'r' %}
```r
source("code/analysis.R")
```
{% elif language == 'stata' %}
```stata
do code/analysis.do
```
{% else %}
```bash
python code/analysis/main.py
```
{% endif %}

## Outputs

- Figures are saved to `results/figures/`
- Tables are saved to `results/tables/`
- Presentations are saved to `results/presentations/`

## Data

Project data is stored in the `data/` directory and tracked with DVC.

Created: {{ created_at }}
{% if author %}Author: {{ author }}{% endif %}
{% if organization %}Organization: {{ organization }}{% endif %}

---
This project was created by and is managed by [mintd](https://github.com/cooper-lab/mint).
