# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
# `downloads/` is also mintd's enclave-pull staging root. Ignore the bulk
# data but keep DVC pointers trackable (dvc import refuses to write a
# git-ignored `.dvc` file) — mirrors the `/data/**` negation block below.
downloads/**
!downloads/**/
!downloads/**/*.dvc
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
.python-version

# celery beat schedule file
celerybeat-schedule

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# R / renv
# Ignore renv library (installed packages) but keep lockfile
renv/library/
renv/staging/
renv/sandbox/
renv/cellar/
renv/local/
renv/cache/
renv/python/
.Rhistory
.RData
.Ruserdata

# Data files (let DVC handle these)
# Use data/** (not data/) so git still traverses subdirectories,
# allowing negation patterns for .dvc files to work.
/data/**
!/data/**/
!/data/**/.gitkeep
!/data/**/*.dvc

# Enclave transfer archives and the extraction staging dir. Restricted data
# crosses the air gap through here; it must never reach git. Landing receipts
# under transfers/received/ ARE tracked — they are the audit record when a
# transfer is landed without mintd.
/transfers/**
!/transfers/**/
!/transfers/received/**
/incoming/

# Results (let DVC handle these)
/results/**
!/results/**/
!/results/**/.gitkeep
!/results/**/*.dvc

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# mintd init's crash marker. Present only while an init is incomplete; a
# rerun of the same `mintd init` command resumes from it and removes it.
.mintd-init-incomplete

# Temporary files
*.tmp
*.temp

# Stata batch return-code sentinels (written by run_stata.sh/_mintd_run.do)
logs/.rc_*

# -----------------------------------------------------------------------------
# FILES THAT SHOULD BE COMMITTED (do NOT add to gitignore):
# -----------------------------------------------------------------------------
# requirements.txt        - Python minimum version constraints
# requirements-lock.txt   - Python pinned versions (generate with pip freeze)
# renv.lock              - R package lockfile (generate with renv::snapshot())
# DESCRIPTION            - R package metadata
# stata-packages.txt     - Stata package dependencies
# -----------------------------------------------------------------------------
