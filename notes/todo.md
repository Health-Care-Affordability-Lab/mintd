1. looking for stata or stata-mp from comand line, make this part of the mint setup to make sure that it can run a which enfoces how dvc.yaml file is written 
2. scripts should be run from they live, i.e. we should cd src && stata-mp -b do ingest.do`. We should be aware of whether this is a windows or mac environment and the code should reflect that. 
3. We should reflact whether these are windows or mac for everything implemented and make correct changes based on that.
